# proto — Python prototypes

Rapid-iteration prototypes and the correctness **oracle** for code later ported
to Rust in `../core`. See the root `README.md` for setup and the roadmap in
`../docs/eda_platform_roadmap.md`.
