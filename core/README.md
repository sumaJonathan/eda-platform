# core - Rust product core

Performance-critical, memory-safe modules for the Unified EDA Platform. Modules
graduate here from the Python prototypes in `../proto` once stable, validated
against the Python version as an oracle.
